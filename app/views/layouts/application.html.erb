<!DOCTYPE html>
<html>
<head>
  <title>MonkeyPost</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="page-wrap">

  <!--Header-->

  <header class="user-nav row-fluid navbar navbar-default navbar-fixed-top">
    <div class="container">
      <div class="navbar-header">
        <%= link_to "Monkey Post", root_path, :class=>"navbar-brand" %>
      </div>
      <div class="navbar-collapse collapse">
        <ul id="primary-nav" class="nav navbar-nav"></ul>
        <nav id="user_nav" class="nav navbar-nav navbar-right">
          <% if user_signed_in? %>
            <p class="signed_in_top">Signed in as <%= current_user.email %>. Not you?
            <%= link_to "Sign out", destroy_user_session_path, :method => :delete %></p>
          <% else %>
            <%= link_to "Sign up", new_user_registration_path %> or <%= link_to "sign in", new_user_session_path %>
          <% end %>
        </nav>
      </div>
    </div>
  </header>
  <div class="main-content container">
    <div class="row-fluid">
      <%= yield %>
    </div>
    <aside>
    </aside>
  </div>  
</div>
</body>
</html>
